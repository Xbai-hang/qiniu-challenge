# Frontend

Vue 3 + TypeScript + Vite frontend for the Voice Calendar AI Native project.

## Commands

```bash
npm install
npm run dev
npm run build
```

The development server defaults to `http://localhost:5173`.
